this file would include the code we used to train our GAN models, feature fusion network model.
note: we have two types of GAN models the pix2pix is the first one which is not used at the end and the second one is a pix2pix cycle-gan
note: the validation is also with the training of the cycle gan
